# Shell aliases

alias yt-dlp-b='yt-dlp -f "bestvideo[ext=mp4]+bestaudio[ext=m4a]/best[ext=mp4]/best"'
alias brewup='brew update; brew upgrade; brew cleanup --prune=all; brew autoremove; brew doctor'
